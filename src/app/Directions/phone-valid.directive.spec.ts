import { PhoneValidDirective } from './phone-valid.directive';

describe('PhoneValidDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneValidDirective();
    expect(directive).toBeTruthy();
  });
});
